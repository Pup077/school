(() => { const root=document.querySelector('[data-exam-type]');if(!root)return;const type=root.dataset.examType,list=root.querySelector('[data-exam-list]');const esc=(v='')=>String(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));fetch(`public_exams.php?type=${type}`,{cache:'no-store'}).then(r=>r.json()).then(d=>{if(!d.ok)throw 0;list.innerHTML=d.posts.length?d.posts.map(p=>`<article class="exam-list-item">${p.coverImage?`<a class="exam-cover" href="exam-detail.php?id=${encodeURIComponent(p.id)}"><img src="${esc(p.coverImage)}" alt="${esc(p.title)}" loading="lazy"></a>`:`<div class="exam-cover exam-cover-empty">ไม่มีภาพปก</div>`}<div><h3><a href="exam-detail.php?id=${encodeURIComponent(p.id)}">${esc(p.title)}</a></h3><p>${esc(p.summary)}</p><a class="library-button exam-more" href="exam-detail.php?id=${encodeURIComponent(p.id)}">อ่านเพิ่มเติม →</a></div></article>`).join(''):'<p class="library-empty">ยังไม่มีข้อมูลที่เผยแพร่</p>';}).catch(()=>list.innerHTML='<p class="library-empty">โหลดข้อมูลไม่สำเร็จ</p>'); })();
